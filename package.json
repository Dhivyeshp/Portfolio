{
  "name": "portfolio-next",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "dev:fresh": "powershell -Command \"if (Test-Path .next) { Remove-Item -Recurse -Force .next }; npx next dev\"",
    "build": "next build",
    "start": "next start",
    "clean": "powershell -Command \"if (Test-Path .next) { Remove-Item -Recurse -Force .next }\""
  },
  "dependencies": {
    "@react-three/drei": "^10.7.7",
    "@react-three/fiber": "^9.5.0",
    "@vercel/analytics": "^2.0.1",
    "framer-motion": "^12.6.3",
    "gsap": "^3.14.2",
    "next": "^15.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "three": "^0.183.2"
  }
}
